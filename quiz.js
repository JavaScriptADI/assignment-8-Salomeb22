// 1. The W3C Document Object Model (DOM) is a platform and language-neutral interface that allows programs and scripts to dynamically access and update the content, structure, and style of a document.
// 2. The window is the first thing loaded into the browser. The document object is defined as any document that gets loaded inside the window object.
// 3. The querySelector() method returns the first element that matches a CSS selector. To return all matches (not only the first), use the querySelectorAll() instead.
// 4. The innerHTML property returns: The text content of the element, including all spacing and inner HTML tags. The innerText property returns: Just the text content of the element and all its children, without CSS hidden text spacing and tags, except <script> and <style> elements.
// 5. createElement() — create a new element.
// 6. appendChild() — attach the new text node to the element.
// 7. example : const element = document.getElementById("demo");
//              element.remove();
// 8. toggle() method that will add a class if it's not present or remove it if it is.
// 9. example : button.style.width = '100%';
// 10. example : const collection = document.body.children;